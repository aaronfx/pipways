services:
  - type: web
    name: pipways-api
    runtime: python
    plan: standard
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn backend.main:app --host 0.0.0.0 --port $PORT
    autoDeploy: true
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0

      # ── Database ───────────────────────────────────────────────────────────
      - key: DATABASE_URL
        fromDatabase:
          name: pipways-db
          property: connectionString

      # ── Security ───────────────────────────────────────────────────────────
      - key: SECRET_KEY
        generateValue: true

      # ── App URL (required by Paystack callback + email links) ──────────────
      - key: APP_BASE_URL
        value: "https://pipways.com"   # ← change to your real domain

      # ── AI / OpenRouter ────────────────────────────────────────────────────
      - key: OPENROUTER_API_KEY
        sync: false
      - key: OPENROUTER_MODEL
        value: "anthropic/claude-3.5-sonnet"
      - key: OPENROUTER_VISION_MODEL
        value: "anthropic/claude-3.5-sonnet"
      - key: ANTHROPIC_API_KEY
        sync: false

      # ── Paystack (Nigerian payments) ───────────────────────────────────────
      # Get these from https://dashboard.paystack.com/#/settings/developers
      - key: PAYSTACK_SECRET_KEY
        sync: false   # paste sk_live_xxx in Render dashboard
      - key: PAYSTACK_PUBLIC_KEY
        sync: false   # paste pk_live_xxx in Render dashboard

      # ── Email (Mailgun recommended for Nigeria) ────────────────────────────
      # Sign up at https://mailgun.com — free tier = 1,000 emails/month
      - key: EMAIL_PROVIDER
        value: "mailgun"              # or "smtp"
      - key: MAILGUN_API_KEY
        sync: false                   # paste key in Render dashboard
      - key: MAILGUN_DOMAIN
        value: "mg.pipways.com"       # ← your Mailgun sending domain
      - key: EMAIL_FROM_NAME
        value: "Pipways"
      - key: EMAIL_FROM_ADDRESS
        value: "noreply@pipways.com"  # ← must match your Mailgun domain

      # ── SMTP fallback (if not using Mailgun) ──────────────────────────────
      # Uncomment and fill in if EMAIL_PROVIDER=smtp
      # - key: SMTP_HOST
      #   value: "smtp.gmail.com"
      # - key: SMTP_PORT
      #   value: "587"
      # - key: SMTP_USER
      #   sync: false
      # - key: SMTP_PASSWORD
      #   sync: false

      # ── CORS ───────────────────────────────────────────────────────────────
      - key: ALLOWED_ORIGINS
        value: "*"

    healthCheckPath: /health

databases:
  - name: pipways-db
    databaseName: pipways
    plan: standard
    ipAllowList: []
